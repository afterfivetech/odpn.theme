odpn.theme Installation
-----------------------

To install odpn.theme using zc.buildout and the plone.recipe.zope2instance
recipe to manage your project, you can do this:

* Add ``odpn.theme`` to the list of eggs to install, e.g.:

    [buildout]
    ...
    eggs =
        ...
        odpn.theme

* Re-run buildout, e.g. with:

    $ ./bin/buildout

